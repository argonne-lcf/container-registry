Bootstrap: docker
From: postgres:latest

%environment
export POSTGRES_USER=admin
export POSTGRES_PASSWORD=adminpwd123

%post
apt-get update && apt-get install apt-file -y && apt-file update && apt-get install vim -y

%startscript
exec postgres "$@"
